# Prax Runtime Rule

- Prefer `prax` commands and lifecycle over legacy entrypoints.
- Treat the native runtime as the canonical execution path.
- Treat Claude Code support as ecosystem integration, not subprocess orchestration.
- If a task is non-trivial, maintain explicit task state and verification steps.
