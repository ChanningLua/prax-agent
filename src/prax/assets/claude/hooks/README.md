# Prax Claude Hooks

This directory reserves the Prax-managed Claude hook surface.

Current release installs only managed placeholders so `doctor` / `repair` /
`uninstall` can reason about ownership deterministically.
